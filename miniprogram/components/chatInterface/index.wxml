<!--components/chatInterface/index.wxml-->
<view class="chat-interface">
  <!-- 消息预览区域 - 只显示最后 2 条消息 -->
  <view class="messages-preview" bindtap="onMessagesClick">
    <view 
      class="message-item message-{{item.role}}"
      wx:for="{{messages.slice(-2)}}" 
      wx:key="index"
    >
      <view class="message-bubble">
        <text class="message-text">{{item.content}}</text>
      </view>
    </view>
  </view>

  <!-- 输入区域 -->
  <view class="input-area">
    <view class="input-box">
      <input
        class="input-field"
        type="text"
        value="{{input}}"
        placeholder="Message"
        placeholder-class="input-placeholder"
        bindinput="onInput"
        bindconfirm="onConfirm"
      />
      <view 
        class="send-btn {{input ? '' : 'disabled'}}"
        bindtap="onSend"
      >
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="m22 2-7 20-4-9-9-4 20-7z" fill="white"/>
          <path d="m22 2-7 20-4-9-9-4 20-7z" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </view>
    </view>
  </view>
</view>