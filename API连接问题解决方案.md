# API 连接问题解决方案

## 问题现象
微信小程序调用 API 时出现 `request:fail timeout` 错误

## 原因分析
微信小程序无法直接访问 `localhost` 或 `127.0.0.1`，因为：
1. 小程序运行在独立的运行时环境中
2. `localhost` 指向小程序自身，而不是开发机器
3. 必须使用实际的网络 IP 地址

## 解决方案

### 方案 1：使用本机 IP 地址（推荐）

1. **获取本机 IP 地址**：
   ```bash
   # macOS/Linux
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
   # Windows
   ipconfig
   # 查找 IPv4 地址，通常是 192.168.x.x 或 10.x.x.x
   ```

2. **修改 `miniprogram/services/api.js`**：
   ```javascript
   const LOCAL_IP = '你的本机IP地址'; // 例如: '192.168.1.100'
   const API_BASE = `http://${LOCAL_IP}:8000`;
   ```

3. **确保后端服务允许外部访问**：
   ```python
   # llm/main.py 中应该是：
   uvicorn.run("main:app", host="0.0.0.0", port=8000)
   ```

4. **检查防火墙**：
   - macOS: 系统偏好设置 -> 安全性与隐私 -> 防火墙
   - 确保允许 Python 或终端应用的网络连接

### 方案 2：使用微信开发者工具的网络代理

1. 打开微信开发者工具
2. 设置 -> 代理设置 -> 使用系统代理设置
3. 或者配置自定义代理

### 方案 3：使用 localhost 代理（高级）

如果以上方案都不行，可以考虑：
1. 使用 Charles 或 Fiddler 等代理工具
2. 配置反向代理将 localhost 映射到可访问的地址

## 验证步骤

1. **测试后端服务是否可访问**：
   ```bash
   # 从同一网络的其他设备访问
   curl http://你的IP:8000/health
   ```

2. **检查微信开发者工具设置**：
   - 设置 -> 项目设置 -> 本地设置
   - 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"

3. **查看控制台日志**：
   - 微信开发者工具 -> 调试器 -> Console
   - 查看是否有详细的错误信息

## 常见问题

### Q: IP 地址获取后仍无法连接？
A: 
1. 确保手机/小程序和电脑在同一网络
2. 检查防火墙设置
3. 确认后端服务监听在 `0.0.0.0` 而不是 `127.0.0.1`

### Q: 每次 IP 地址都变化？
A: 
- 可以配置静态 IP 地址
- 或者在路由器中设置 DHCP 保留

### Q: 生产环境怎么办？
A: 
- 使用实际的服务器地址
- 配置 HTTPS
- 在小程序后台配置服务器域名白名单

## 当前配置

检测到的本机 IP: `172.16.23.57`

如果该 IP 无法访问，请尝试：
1. 检查网络配置
2. 使用其他网络接口的 IP 地址
3. 确保后端服务监听在 `0.0.0.0:8000`

